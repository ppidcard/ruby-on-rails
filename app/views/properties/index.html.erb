<% if @suburb %>
  <h1 class="text-center mt-4">Listing properties in <%= @suburb.name %></h1>
<% else %>
  <h1 class="text-center mt-4">Listing all properties</h1>
<% end %>

<div class="flickr_pagination">
  <%= will_paginate @properties, :container => false %>
</div>
<%= render 'property' %>
<div class="flickr_pagination mb-4">
  <%= will_paginate @properties, :container => false %>
</div>